version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: infiplus-whatsapp-calling-agent
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
